<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lisbeth.rest">

    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Main2Activity"
            android:parentActivityName=".Login" />
        <activity
            android:name=".Book"
            android:parentActivityName=".Main2Activity" />
        <activity
            android:name=".Hire"
            android:parentActivityName=".Fetching" />
        <activity android:name=".Societies" />
        <activity
            android:name=".Fetching"
            android:parentActivityName=".Book" />
        <activity android:name=".Update" />
        <activity android:name=".GetSMS" />
        <activity android:name=".Orbituaries" />
        <activity android:name=".InfoOrbirtuaries"></activity>
    </application>

</manifest>